val for = require("forLoop.brg");

val array = [5, 78.0, 21];
val l = arr.length(array);

val condition = lm(i) return i < l; end lm;
val action = lm(i) return i + 1; end lm;

for(0, condition, action, lm(i)
    out(array[i]);
end lm);
